---
import { Image } from 'astro:assets';

const { slug, title, image, tags } = Astro.props;
---

<figure class="shrink-0 w-full relative">
  <div class="relative">
    <a href={`/project/${slug}`} class="no-underline relative block" aria-label={title}>
      <span class="relative block transition duration-500 ease-in-out transform rounded hover:scale-105 aspect-square overflow-hidden">
        <Image width={384} class="relative block object-cover object-center w-full h-full" loading="lazy" alt={title} src={image}>
      </span>
    </a>
  </div>
  <div class="pb-0 px-0 py-2">
    <h3 class="text-black text-xl my-2 font-heading font-light tracking-wide uppercase">
      <a href={`/project/${slug}`} class="no-underline relative inline-block" aria-label={title}>{title}</a>
    </h3>
    <p class="text-sm mb-6 m-0 text-zinc-500 relative">{tags.join(', ')}</p>
  </div>
</figure>
